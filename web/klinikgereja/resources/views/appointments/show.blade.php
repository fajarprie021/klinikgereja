<x-app-layout>
    <x-slot name="header"><h2 class="font-semibold text-xl text-gray-800">Appointment Detail</h2></x-slot>
    <div class="py-6 max-w-3xl mx-auto sm:px-6 lg:px-8"><div class="bg-white p-6 shadow-sm sm:rounded-lg space-y-2"><p><strong>Queue:</strong> {{ $appointment->queue_number }}</p><p><strong>Date:</strong> {{ $appointment->date->format('Y-m-d') }}</p><p><strong>Patient:</strong> {{ $appointment->patient->name }}</p><p><strong>Doctor:</strong> {{ $appointment->doctor->name }}</p><p><strong>Status:</strong> {{ $appointment->status }}</p>@can('updateStatus', $appointment)<div class="flex gap-3 pt-3">@foreach (['CHECKED','DONE','CANCELLED'] as $next)<form method="POST" action="{{ route('appointments.status', [$appointment, 'status' => $next]) }}">@csrf<button class="text-indigo-600" type="submit">{{ $next }}</button></form>@endforeach</div>@endcan @can('update', $appointment)<a class="text-indigo-600" href="{{ route('appointments.edit', $appointment) }}">Edit</a>@endcan</div></div>
</x-app-layout>
