@if ($errors->any())<div class="text-red-600">{{ $errors->first() }}</div>@endif
<label class="block"><span class="text-gray-700">Patient</span><select class="mt-1 block w-full rounded border-gray-300" name="patient_id" required><option value="">Select patient</option>@foreach ($patients as $patient)<option value="{{ $patient->id }}" @selected((string) old('patient_id', $appointment->patient_id ?? '') === (string) $patient->id)>{{ $patient->patient_number }} - {{ $patient->name }}</option>@endforeach</select></label>
<label class="block"><span class="text-gray-700">Doctor</span><select class="mt-1 block w-full rounded border-gray-300" name="doctor_id" required><option value="">Select doctor</option>@foreach ($doctors as $doctor)<option value="{{ $doctor->id }}" @selected((string) old('doctor_id', $appointment->doctor_id ?? '') === (string) $doctor->id)>{{ $doctor->name }}</option>@endforeach</select></label>
<label class="block"><span class="text-gray-700">Date</span><input class="mt-1 block w-full rounded border-gray-300" type="date" name="date" value="{{ old('date', isset($appointment) ? $appointment->date->format('Y-m-d') : now()->format('Y-m-d')) }}" required></label>
